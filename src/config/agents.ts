// The agent id the magic-wand prompt (AIPrompt) creates once and reuses on
// every subsequent submission. Reserved so a user can't collide with it via
// the manual "Create a new agent" dialog.
export const DEFAULT_AGENT_ID = "default-agent";
