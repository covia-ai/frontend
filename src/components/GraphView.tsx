"use client"

import { convertAgentDataToReactFlow } from "@/lib/agentDiagramUtil";


const GraphView = (props:any) => {

  convertAgentDataToReactFlow(agentDa)
  return (
    <div className="flex flex-col">
       <div className="flex flex-row">
          
        </div>
    </div>
  );
};

export default GraphView;